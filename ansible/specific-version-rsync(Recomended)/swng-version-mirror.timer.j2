[Unit]
Description=Run CloudLinux {{ cloudlinux_version }} SWNG Mirror Sync Every {{ sync_interval_hours }} Hours

[Timer]
OnCalendar={{ timer_schedule }}
Persistent=true

[Install]
WantedBy=timers.target
